function Filter({ value, onChange }) {

  // const handleClick = (key, e) => {
  //   e.preventDefault();
  //   onChange(key);
  // };

  return (
    <div className="panel-tabs">
      <table id="customers1">
          <tr>
            <td>ID</td>
            <td>NAME</td>
            <td>GRADE</td>
          </tr>
        </table>
     
    </div>
  );
}

export default Filter